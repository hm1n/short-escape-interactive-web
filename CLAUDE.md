# 쇼츠 이탈 설명 페이지 — 기획서 v2.2

> 이 문서는 기획서이자 작업 지침이다. 카피·구조·수치를 바꾸려면 여기부터 고치고 HTML을 따라 맞춘다.
> 산출물: `shorts-escape-scroll.html` (단일 파일 · 약 232KB)

---

## 1. 문제 정의

"쇼츠를 못 끊는다"는 이야기는 이미 넘친다. 대부분 둘 중 하나로 흐른다.

- **훈계형** — 도파민 중독이다, 뇌가 망가진다. 과장이 섞여 있고, 읽고 나면 자책만 남는다.
- **팁형** — 앱 지워라, 타이머 걸어라. 왜 안 되는지를 설명하지 않아서 다음 날 원상복구된다.

빠져 있는 건 **구조 설명**이다. 왜 그 순간에 손이 가는지, 왜 멈출 수 없는지, 왜 다시 켜는지를 각각 다른 힘으로 분리해 보여주면, 독자는 자책 대신 이해를 가져간다.

**한 줄 정의 —** 쇼츠를 못 끊는 이유를 "켤 때 / 볼 때 / 다시 켤 때" 세 순간으로 분해해, 읽는 사람이 스크롤로 직접 겪게 만드는 단일 HTML 페이지.

---

## 2. 타깃 · 톤 · 목표

| 항목 | 내용 |
|---|---|
| 타깃 | 20대. 이미 문제를 자각하고 있고, 훈계에는 반발하는 층 |
| 톤 | 단정적이되 과장 없음. 존댓말 본문 + 반말 헤드라인. **훈계 금지, 처방 금지** |
| 독자가 얻는 것 | "내가 약한 게 아니라 구조가 그렇게 생겼구나"라는 이해 |
| 성공 조건 | 끝까지 스크롤한다 → 마지막 영수증에서 "아 나도 방금 이걸 했네" 하고 멈칫한다 |
| 실패 신호 | 중간 이탈, "그래서 어쩌라고", "뻔한 소리" |

**하지 않는 것:** 해결책 제시, 절제 권유, 통계 과장, 유튜브 브랜드 자산 사용.
결말은 처방이 아니라 **면죄**다 — "그래서 우리는 결국 정상이다."

> **알고 쓰는 예외 —** 히어로 부제목은 "우리가 쇼츠에 중독되는 이유"다. §1이 안티패턴으로 규정한 "중독" 프레임과 부딪히지만, 첫 화면에서 주제를 한 번에 전달하는 값이 더 크다고 판단해 유지한다. 본문에서는 이 단어를 쓰지 않는다 — 제목에서만 끌어오고 안에서는 구조로 설명한다. 톤 충돌이 문제가 되면 대안은 "우리가 쇼츠를 못 끊는 이유".

---

## 3. 핵심 컨셉 — 매체가 곧 주제

**스크롤 행위 자체가 쇼츠 스와이프와 동일 구조다.** 독자는 설명을 읽는 동시에 설명 대상을 수행한다.

v2.2에서 전 화면을 **스마트폰 카드뷰**로 바꾸면서 이 컨셉이 한 단계 더 문자화됐다. 독자는 폰 화면 안에서 폰 이야기를 읽고, 마지막에 그 화면이 뒤집히는 걸 본다.

모든 판단은 이 질문을 통과해야 한다:
> *"이 요소는 독자가 지금 하고 있는 행동과 연결되는가?"*

연결되지 않는 요소는 아무리 좋아도 뺀다. 이게 이 페이지가 일반적인 아티클과 갈리는 지점이다.

---

## 4. 서사 구조 — 3막 + 고리

원고의 뼈대는 세 순간이고, 그 셋이 마지막에 하나의 고리로 닫힌다.

```
0막 시작    5분 → 1시간. 의지 아닌 구조. 세 순간 예고
─────────────────────────────────────────────
1막 켤 때   재미보다 회피가 먼저 → 켜는 것도 습관이 됨
2막 볼 때   모름 · 피크 · 단위 · 정지 신호 없음 → "멈출 순간을 뒤로 미룬다"
3막 다시    빠른 해결책으로 학습 → 알고리즘이 개인화로 되받음
─────────────────────────────────────────────
4막 고리    ①~⑥이 닫힌다 (여기서 HUD 실시간 삽입)
5막 회수    영수증 → 색 반전 → "정상이다"
```

### 화면 구성 (12씬)

| # | 라벨 | 막 | 헤드라인 | 인터랙션 |
|---|------|----|---------|---------|
| — | 히어로 | 0 | 5분만 보려다 한 시간이 지나 있다<br>*부제: 우리가 쇼츠에 중독되는 이유* | 피드 마크 · **배경 사진 20%** |
| 01 | 방아쇠 | 1 | 재미보다 먼저 작동하는 게 있다 | **상황 칩 4개** (기본 선택 있음) |
| 02 | 연결 | 1 | 쇼츠를 켜는 것도 습관이 된다 | 큐→루틴 4행 + **실시간 반복 수** |
| 03 | 모름 | 2 | 다음에 뭐가 나올지 모른다 | **"다음 영상" 버튼** (60/30/10) |
| 04 | 피크 | 2 | 가장 좋은 순간은 넘기기 직전이다 | SVG 곡선 드로잉 |
| 05 | 단위 | 2 | 영상 하나는 짧다 합계만 길다 | 슬라이더 + **조사기관 프리셋 3개** |
| 06 | 정지 신호 | 2 | 끝났다고 말해주는 게 없다 | **진행바 회수 + HUD 점멸** |
| 07 | 재방문 | 3 | 몇 초 만에 듣는 해결책이 된다 | **01 칩 회수** (문장 재작성) |
| 08 | 개인화 | 3 | 피드는 당신보다 당신을 빨리 배운다 | 좁혀지는 도트 그리드 |
| 09 | 고리 | 4 | 결국 하나의 고리가 된다 | **③에 실시간 스와이프 수** |
| 10 | 기록 | 5 | 방금 그 고리를 여기서 겪으셨습니다 | **영수증 6행 + PNG 저장** |
| 11 | 마무리 | 5 | 그래서 우리는 결국 정상이다 | **화면 흑→백 반전** |

**순서는 인과다.** 임의로 재배치하지 않는다. 특히 06(정지 신호)이 2막 끝에 오는 이유는, 그 직후 "그렇게 오래 본 행동이 다음 행동을 만든다"로 3막에 넘겨야 하기 때문이다.

**02의 헤드라인은 메커니즘을 명명한다.** "상황이 부른다"는 현상 묘사였고, "습관이 된다"는 그 현상의 이름이다. 1막이 "회피 동기 → 습관화"로 닫혀야 3막의 "빠른 해결책으로 학습"과 층이 갈린다. 라벨 `연결`은 습관이 만들어지는 방식(큐-루틴 연합)을 가리키므로 그대로 둔다.

---

## 5. 시그니처 장치 — 절대 훼손 금지

### 5-1. HUD ↔ 영수증 회수 루프 (**최우선**)

이 페이지가 다른 아티클과 다른 단 하나의 이유다. 상단 HUD가 처음부터 끝까지 독자를 측정하고, 마지막에 그 기록을 되돌려준다.

**측정 항목:** 체류 시간(탭 활성 시에만) · 스와이프 수(화면 전환 횟수) · 구간별 체류 · 슬롯 클릭 수 · 선택한 방아쇠 · 슬라이더로 잡은 하루 개수

**심고 회수하는 지점 — 이 사슬이 끊기면 페이지가 무너진다:**

| 지점 | 역할 | 구현 |
|---|---|---|
| 히어로 | **예고** — "당신의 스와이프 수와 체류 시간을 세고 있습니다" | `.hero-meta` |
| 01 방아쇠 | **심기** — 칩 선택을 저장 | `applyTrigger()` `trigger` |
| 02 연결 | **1차 회수** — "문단이 끝난다 → 스와이프한다 · N회 반복". 01에서 고른 칩과 같은 행에 "← 당신이 고른 것" | `#chainLive` `.chain div.sel` |
| 03 모름 | **심기** — 슬롯 클릭 수 누적 | `slotPresses` |
| 05 단위 | **심기** — 슬라이더 값과 1년 환산 | `cnt` `yearHours` |
| 06 정지 신호 | **2차 회수** — "화면 맨 위를 보세요. 피드에는 이게 없습니다" + HUD 1회 점멸(0.9초 뒤, 3펄스) | HUD 진행바 · `.hud.flash` |
| 07 재방문 | **3차 회수** — 01에서 고른 방아쇠를 문장에 되꽂음 | `#recallLine` |
| 09 고리 | **4차 회수** — 고리 ③ 옆에 지금까지의 실시간 스와이프 수 | `#loopSwipe` |
| 10 기록 | **전면 회수** — 영수증 6행 + 마무리 카피 + PNG 저장 | `#receipt` `#saveBtn` |
| 11 마무리 | **최종 회수** — "여기엔 끝이 있었습니다. 진행바가 지금 N%입니다" | `#closeFill` |

회수는 5단계로 점층한다(현재 행동 → 진행바 → 개인 선택 → 실시간 숫자 → 전체 기록). 중간 단계를 빼면 마지막 영수증이 갑자기 튀어나온 장치처럼 보인다.

**02의 위치가 중요하다.** 첫 회수를 2막이 아니라 1막 안에 두면, 독자는 "이 페이지가 진짜로 세고 있구나"를 초반에 확인한다. 그 확인이 있어야 뒤의 회수들이 장치가 아니라 기록으로 읽힌다.

**06의 HUD 점멸**은 유일하게 페이지가 독자의 시선을 끌어당기는 지점이다. 카피가 "화면 맨 위를 보세요"라고 말하는 순간에만 쓰고, 다른 곳에서는 쓰지 않는다. `prefers-reduced-motion`에서는 JS 단계에서 아예 등록하지 않는다.

**HUD는 카드 폭에 정렬한다.** 진행바가 카드 위에 얹힌 상태바처럼 읽혀야 "이 화면의 진행바"라는 06의 회수가 성립한다.

**영수증 PNG 저장**은 canvas로 직접 그린다. 화면 DOM을 캡처하는 게 아니라 같은 데이터를 다시 그리는 방식이라 의존성이 없다. 만들어지는 경로는 전부 로컬이며, 공유 여부는 독자가 결정한다.

> **새 인터랙션을 추가하면 반드시 영수증에 한 행을 추가할지 먼저 판단한다.** 회수되지 않는 인터랙션은 이 페이지에서 장식이다.

#### 기본값 표기 규칙 (v2.2 신설)

인터랙션에 기본값을 두면 **영수증에서 기본값 상태와 직접 선택을 구분해 표기한다.**

| 항목 | 손대기 전 | 직접 선택 후 |
|---|---|---|
| 방아쇠 | `심심할 때 (기본값)` | `심심할 때` |
| 하루 개수 | `110개 (기본값)` | `253개 · 1년 770시간` |

기본값은 **"누를 수 있다"는 걸 알리기 위해** 존재한다(아무것도 선택돼 있지 않으면 칩이 버튼으로 안 읽힌다). 하지만 영수증은 "당신이 한 일"의 기록이므로, 하지 않은 선택을 한 것처럼 적으면 §8의 정직 원칙과 회수 장치의 신뢰도가 동시에 깨진다. **기본값이 있는 인터랙션을 새로 만들면 이 표기를 같이 만든다.** 화면과 PNG 양쪽 모두 적용한다.

### 5-2. 11번의 색 반전

배경이 흑에서 백으로 뒤집히는 지점은 **딱 한 번**, 마지막 화면이다.

**카드뷰 전환 후 반전의 의미가 더 선명해졌다 — 화면(카드 내부)만 희게 뒤집히고 기기(베젤)는 검게 남는다.** 폰은 그대로인데 화면이 바뀐 그림이고, "피드에서 나온다"를 색으로 겪게 한다는 원래 의도가 문자 그대로 성립한다. 베젤을 밝게 바꾸면 이 효과가 사라진다.

배치 근거: 09 고리(구조가 닫힘) → 10 영수증(가장 어두운 지점, 당신도 겪었다) → **11 반전(해소)**. 반전이 결론의 감정과 붙어야 작동한다. 앞으로 당기거나 두 번 쓰면 효과가 사라진다.

### 5-3. 09번의 "인과관계는 논쟁 중" 문단

이 주제의 콘텐츠는 대부분 과장으로 흐른다. 이 한 줄이 나머지 주장 전체의 신뢰도를 지탱한다. **삭제 금지, 톤 완화 금지.** 가장 센 주장(고리 다이어그램) 바로 옆에 있어야 의미가 있다.

### 5-4. 유튜브 브랜드 자산 재현 금지

이름 언급은 괜찮다. 로고·UI 클론은 안 된다. 추상 도형과 실사 사진(§6)만 쓴다.

---

## 6. 디자인 시스템

토큰 변경은 `:root`에서만 한다.

```
--void   #0B0A0F   기본 배경 = 카드 바깥 (보라 기운 도는 near-black)
--screen #141119   카드 내부 = 폰 화면
--panel  #1C1824   카드 안의 카드 (슬롯·계산기 셀 등)
--wire   #2E2837   헤어라인 · 스피커 그릴
--glow   #F2EFE8   본문 흰색
--dim    #8A8296   보조 텍스트
--pulse  #FF4A35   포인트 (코랄레드) — 아껴 쓸 것
--cool   #7FD8E8   차트의 "실제 만족" 곡선에만 사용
--day    #F2EFE8   반전 구간 화면 + 영수증 용지

--card-w   808px   카드 폭
--card-r    38px   카드 라운드
--card-gap  60px   카드 위 여백 (HUD 자리)
```

- 타이포: 헤드라인 Black Han Sans(400 고정) / 본문 IBM Plex Sans KR 300 / 숫자·계기판 IBM Plex Mono
- `--pulse`는 CTA·강조 수치·회수 지점에만. 여기저기 쓰면 회수 순간의 신호가 죽는다.
- **한글은 어절 단위로 끊는다.** `body`에 `word-break:keep-all` + `overflow-wrap:break-word`. 영수증 PNG의 canvas 줄바꿈도 같은 규칙을 따른다(§7).

### 6-1. 카드 = 스마트폰 화면

**콘텐츠는 전부 카드(`.scene-in`) 안에서만 산다. 카드 밖은 배경색뿐이다.** 배경 사진도 카드 안에만 들어간다.

폰으로 읽히게 하는 요소는 셋뿐이다. 더 늘리지 않는다 — 크롬이 늘어나면 콘텐츠가 밀린다.

| 요소 | 값 | 비고 |
|---|---|---|
| 베젤 | `border:9px solid #07060A` | 화면이 아니라 **기기**로 읽히게 하는 유일한 요소. 반전 구간에서도 검게 남는다 |
| 스피커 그릴 | `.scene-in::after` 54×5px, `--wire` | 상단 중앙. 말 안 하고 폰이라고 알려주는 장치 |
| 라운드 | `--card-r` 38px (모바일 28px) | |

여백 — **가로 4배 / 세로 2배**가 기준값이다. 세로까지 4배로 두면 카드 절반이 1화면을 넘긴다.

| 구간 | 카드 패딩 | 카드 위/아래 여백 |
|---|---|---|
| 기본 | `76 / 88 / 64` | 60 / 48 |
| 세로 ≤800px | `58 / 88 / 50` | 52 / 42 |
| 가로 ≤460px | `52 / 22 / 44` | 60 / 48 |

> 좁은 화면에서 가로 88px을 그대로 쓰면 본문이 177px만 남는다. 모바일 값은 **가드**이지 4배 결정의 예외가 아니다.

### 6-2. 카드 안에서 지켜야 할 것

- **콘텐츠는 카드 폭에 맞춘다.** 뷰포트 기준으로 잡으면 카드 안에서 깨진다. `h1`·`h2`의 `clamp()`도 카드 폭 기준으로 상한을 뒀다.
- **폭에 따라 커지면 안 되는 요소는 열 수를 늘린다.** 08 도트 그리드가 그 예다 — 12열 고정이었을 때 카드가 넓어지자 셀이 58px로 커져 카드가 126px 넘쳤다. `repeat(auto-fill, minmax(24px, 1fr))`로 셀 크기를 고정하고 열 수를 폭에 맡긴다.
- **밀도 높은 섹션(05·09·10)은 여백 예산이 빠듯하다.** 카드 여백을 늘리면 이 셋부터 넘친다. 늘려야 한다면 여백이 아니라 이 섹션들의 내부 간격을 먼저 조정한다.

### 6-3. 사진

히어로 배경에만 쓴다. `opacity: .2`, `cover`, `center 38%`, 카드 안쪽(`.scene.hero .scene-in::before`).

**주인공이 아니라 독자가 지금 있는 자리를 깔아두는 용도다.** 20%를 넘기면 본문 가독성이 떨어지고 사진이 주장을 하기 시작한다. 다른 씬에 사진을 추가하지 않는다 — 히어로에만 있어야 "여기서부터 안으로 들어간다"는 신호가 된다.

### 6-4. 컴포넌트 클래스

`.chips`(01) `.chain`·`.chain-live`(02) `.slot`(03) `.chart`(04) `.calc`·`.presets`(05) `.pivot`(06 결론) `.grid`(08) `.loop`(09) `.receipt`·`.rc-actions`(10) `.verdict-line`(11)

영수증 PNG도 같은 팔레트를 쓴다: 용지 `--day`, 본문 `--day-ink`, 보조 `#4A4456`, 점선 `--day-wire`. 화면과 이미지가 달라 보이면 안 된다.

---

## 7. 기술 사양

- 단일 파일 유지가 기본. CSS/JS 인라인, 빌드 스텝 없음. 분리는 실제로 불편해질 때만.
- 외부 의존성은 Google Fonts만. **이미지도 base64로 인라인한다** — 별도 파일로 두면 HTML 한 장만 공유했을 때 배경이 깨진다. 대가로 파일이 50KB → 232KB가 됐다.
  - 히어로 사진: 1200px 폭 / JPEG q50 / data URI 약 172KB. 20% 불투명도로 깔리므로 압축 아티팩트는 보이지 않는다. 더 키울 이유가 없다.
- 스크롤 컨테이너는 `#scroller` (body가 아님). 스크롤 이벤트·진행률 계산 전부 여기 기준.
- `scroll-snap-type: y proximity` — mandatory로 바꾸면 긴 섹션에서 사용자가 갇힌다.
- 높이는 `100dvh` (모바일 브라우저 크롬 대응). `100vh`로 되돌리지 말 것.
- 타이머는 `document.hidden`일 때 멈춘다. 영수증 정확도의 근거이자 푸터에 명시한 약속.
- `prefers-reduced-motion: reduce`에서 모든 애니메이션 무효화 + reveal 요소 강제 표시. **새 애니메이션 추가 시 여기도 추가.**
- 상태는 전부 인메모리. "새로고침하면 사라진다"가 푸터에 명시된 약속이니, localStorage를 쓰려면 카피도 같이 고친다.
- 영수증 PNG는 `canvas` 2배 스케일 · 폭 760pt 고정, 높이는 본문 줄 수로 계산. `document.fonts.ready` 이후에 그린다(폰트 로드 전이면 폴백 글꼴로 찍힌다). 줄바꿈은 **공백 우선**(화면의 `keep-all`과 일치), 공백이 없으면 글자 단위로 폴백하고, 마지막 줄에 한두 글자만 남으면 폭을 줄여 다시 나눈다(`wrapTidy`).
- `navigator.canShare({files})`가 지원될 때만 공유 버튼을 노출한다. 데스크톱에서는 숨은 채로 두는 게 정상이다.

### 함정 두 가지 (둘 다 실제로 밟았다)

- **초기화 순서** — `onScroll()`이 최초 1회 실행되면서 `paintReceipt()`를 부른다. 영수증이 읽는 값(`cnt`, `yearHours` 등)은 그 전에 선언·할당해야 한다. 어기면 스크립트 전체가 그 자리에서 죽고, 증상은 "도트 그리드가 안 그려짐"처럼 엉뚱하게 나타난다. 스크립트 상단 변수 블록에 모아둔 이유가 이것이다.
- **`position` 덮어쓰기** — `.cue`는 이미 `absolute`다. 카드 관련 규칙에서 `position:relative`를 얹으면 화살표가 화면 중앙으로 튄다. z-index만 준다.

---

## 8. 수치와 출처

이 페이지는 09에서 "인과관계는 논쟁 중"이라고 먼저 밝힌다. 그러면 나머지 숫자도 같은 기준을 지켜야 한다. **출처 없는 수치는 쓰지 않거나, 추정치임을 그 자리에서 밝힌다.**

| 수치 | 값 | 출처 | 쓰이는 곳 |
|---|---|---|---|
| 20대 하루 평균 숏폼 이용 | 55분 | 메조미디어 2024 타겟 리포트 | 05 슬라이더 **기본값의 근거** (55분 ÷ 30초 = 110개) |
| Z세대 숏폼 시청 | 평일 126.6분 · 주말 139.0분 | 대학내일20대연구소 정기조사(2025.4) | 05 프리셋 |
| 1회 시청 지속 시간 | 평균 21분 | 컨슈머인사이트(2025) | 05 프리셋 |
| 숏폼 플랫폼 점유 | 유튜브 쇼츠 75% | 컨슈머인사이트(2025) | 푸터 |
| 영상 1개 평균 길이 | 30초 | **추정치** — 쇼츠 길이가 대체로 20~40초에 몰린다는 관측 | 05 환산 계수 |
| 히어로 배경 사진 | — | Rasheed Kemy, Unsplash | 푸터 크레딧 |

- 조사기관마다 55분과 126.6분처럼 두 배 넘게 벌어진다. 이걸 숨기고 하나만 쓰는 대신 **프리셋 3개로 나란히 보여준다.** "조사마다 숫자가 다릅니다"가 그 자체로 이 페이지의 톤이다.
- **개당 30초만 추정치다.** 유일하게 조사값이 아니므로 05 note와 푸터 양쪽에 "추정치이며 조사값이 아닙니다"를 명시한다. 이 표기를 지우지 않는다.
- 출처 전문은 푸터에 둔다. 05 note에 같은 목록을 반복하지 않는다(프리셋 버튼 → note → 푸터 3중 중복이었다).
- 수치를 갱신할 때는 이 표 → HTML의 `data-src` 속성 → 05 note → 푸터 순으로 네 곳을 함께 고친다.
- 사진은 Unsplash 라이선스상 표기 의무가 없지만, 수치 출처를 밝히는 페이지에서 사진만 빼는 건 앞뒤가 안 맞아 크레딧을 남긴다.

---

## 9. 변경 이력

### v1 → v2 (원고 교체)

| 변경 | 내용 | 근거 |
|---|---|---|
| 골격 교체 | 7개 병렬 설명 → **3막 + 고리** | 병렬 나열은 순서를 바꿔도 말이 됐다. 3막은 인과라 못 바꾼다 |
| 신규 01·02 | 진입 동기(회피)와 습관화를 추가 | v1은 "이미 켠 상태"에서 시작해 왜 켜는지가 비어 있었다 |
| 신규 09 | 고리 다이어그램 ①~⑥ | 3막이 닫히는 걸 그림으로 보여줘야 "구조"라는 단어가 성립 |
| 삭제 | v1 08 탈출 카드 4장(처방) | 새 원고의 결론은 처방이 아니라 면죄 |
| 삭제 | v1 07 기준선 | 새 원고에 없는 주장. **단, "논쟁 중" 문단은 09로 이관해 살렸다** |
| 이동 | 색 반전 08 → **11 마무리** | 결론과 붙이니 근거가 더 강해짐 |
| 흡수 | 슬롯머신 vs 피드의 비용 차이 | 별도 씬 대신 03 note로. 씬 수보다 밀도 |

### v2 → v2.1 (남은 작업 반영)

| 변경 | 근거 |
|---|---|
| 05 수치를 출처 있는 값으로 교체 + 프리셋 3개 | 감각값이 09의 정직 원칙과 충돌 |
| 02 "반복 N회"를 실시간 스와이프 수로 연동 | 하드코딩 N은 장식이었다. 회수가 5단이 됨 |
| 영수증에 "하루 개수" 행 추가 | 05가 유일하게 회수되지 않는 인터랙션이었다 |
| 영수증 PNG 저장/공유 | 회수의 마지막 단계를 페이지 밖으로 |
| 06 HUD 1회 점멸 | "화면 맨 위를 보세요"가 카피뿐이라 시선이 안 올라갔다 |

### v2.1 → v2.2 (카드뷰 전환)

| 변경 | 근거 |
|---|---|
| **전 씬 스마트폰 카드뷰** (베젤·스피커 그릴) | §3 컨셉의 문자화. 폰 안에서 폰 이야기를 읽는다 |
| 히어로 부제목 "우리가 쇼츠에 중독되는 이유" | 제목만으로 주제가 안 잡혔다. §2에 예외로 기록 |
| 히어로 배경 사진 20%, base64 인라인 | 첫 화면에서 독자가 있는 자리를 깔아준다 |
| 배경 레이어를 씬 → **카드 내부**로 | 카드 밖은 배경색만 남긴다는 원칙 |
| 11 반전을 **카드 내부에만** 적용 | 기기는 그대로, 화면만 뒤집힘 — 의미가 더 선명해짐 |
| HUD를 카드 폭에 정렬 | 카드 위에 얹힌 상태바로 읽혀야 06 회수가 성립 |
| 카드 폭 808px, 여백 가로 4배·세로 2배 | 좁은 카드에서 05·09가 130px씩 넘쳤다 |
| 08 도트 그리드 `auto-fill` 전환 | 카드가 넓어지자 셀이 커져 126px 넘침 |
| `word-break:keep-all` 전역 + PNG 줄바꿈 동기화 | 한글이 단어 중간에서 잘렸다 |
| **01 칩 기본 선택 + "(기본값)" 표기 규칙** | 아무것도 선택돼 있지 않으면 누를 수 있다는 걸 모른다. 대신 영수증이 거짓말하지 않게 §5-1에 규칙 신설 |
| 02 헤드라인을 "쇼츠를 켜는 것도 습관이 된다"로 | 현상 묘사 → 메커니즘 명명. 1막이 습관화로 닫혀야 3막과 층이 갈린다 |
| 버그 수정 | 초기화 순서로 스크립트 전체 중단 / `.cue` position 덮어쓰기 (§7) |

---

## 10. 남은 작업

- [ ] **모바일(375px) 밀도** — 12개 중 7개 씬이 1화면을 넘긴다. 05가 +240px로 가장 크고 나머지는 45~77px. 카드 폭이 353px까지 줄어드는 구간이라 CSS로는 한계다. 원고를 줄일지, 스크롤을 허용할지 결정 필요 (현재는 허용)
- [ ] **11 마무리의 진행바가 95%로 표시된다** — 아래 푸터까지가 스크롤 길이에 포함돼서다. "여기엔 끝이 있었습니다"의 마지막 한 방이 무뎌진다. 진행률 계산에서 푸터를 빼면 100%가 되지만 HUD 동작 변경이라 보류 중
- [ ] 05 "개당 30초"를 국내 조사값으로 교체 — 현재 유일한 추정치
- [ ] 영수증 PNG의 세로형(9:16) 변형 — 지금은 가로형 1장뿐이라 스토리 공유에 안 맞는다
- [ ] 09 고리에 실시간 값이 ③ 하나뿐. ①·⑤에도 넣을 수 있는지 검토 — 다만 다이어그램이 계기판이 되면 "구조" 인상이 깨지므로 신중히

---

## 11. 협업 규칙

저장소: `hm1n/short-escape-interactive-web`

- **`main`은 항상 배포 가능한 상태로 둔다.** 직접 커밋하지 않고 PR로만 들어간다.
- 브랜치 이름: `feat/` (기능·구조) · `fix/` (버그) · `docs/` (이 문서만 고칠 때)
- **같은 시기에 `shorts-escape-scroll.html`을 둘이 만지지 않는다.** CSS·HTML·JS가 한 파일에 있어서 브랜치를 나눠도 충돌 지점이 겹친다. 섹션 단위(예: 01~06 / 07~11)나 시간 단위로 나눈다.
- **커밋은 회수 사슬 단위로 끊는다.** 01에서 심은 게 07·10에서 회수되는 구조라, 한 지점만 고친 커밋은 그 자체로 깨진 상태일 수 있다. "심기 + 회수"를 한 커밋에 담는다.
- 카피·구조·수치를 바꿨으면 **이 문서를 같은 PR에서 고친다.** 근거가 남지 않으면 다음 사람이 되돌린다.
